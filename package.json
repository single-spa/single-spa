{
  "name": "single-spa",
  "version": "2.0.0-alpha-2",
  "description": "Multiple applications, one page",
  "main": "lib/single-spa.js",
  "scripts": {
    "postinstall": "jspm install",
    "build": "npm run clean && babel src --out-dir lib",
    "prepublish": "npm run build",
    "clean": "rm -rf lib",
    "test": "jasmine",
    "build:canopy": "npm run build && rm -rf ../../workplace/customer-spa/jspm_packages/npm/single-spa@2.0.0-alpha-2 && rsync -av . ../../workplace/customer-spa/jspm_packages/npm/single-spa@2.0.0 --exclude node_modules --exclude .git --exclude --spec --exclude examples"
  },
  "repository": "https://github.com/joeldenning/single-spa",
  "keywords": [
    "single",
    "page",
    "application",
    "spa",
    "multiple",
    "lifecycle",
    "systemjs",
    "jspm"
  ],
  "author": "Joel Denning",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "babel-cli": "^6.7.5",
    "babel-plugin-transform-object-rest-spread": "^6.6.5",
    "babel-polyfill": "^6.7.4",
    "babel-preset-es2015": "^6.6.0",
    "jasmine": "^2.4.1"
  },
  "ignore": [
    "examples"
  ],
  "publishConfig": {
    "registry": "http://registry.npmjs.org"
  }
}
